# 进销存管理系统专业版 - 使用说明

## 📋 系统概述

本系统是一个功能完整、界面专业的进销存管理系统，采用现代化UI设计，支持多用户权限管理，适合中小型企业使用。

## 🚀 系统特点

- ✅ **专业的登录系统**：支持用户认证、权限管理和记住密码
- ✅ **美观的界面设计**：现代化UI，左右布局，Tab页多任务，操作便捷
- ✅ **完整的功能模块**：入库、出库、库存、商品、报表、设置等一应俱全
- ✅ **强大的查询功能**：支持按日期、商品、规格等多条件查询
- ✅ **实时数据统计**：仪表盘显示关键业务指标
- ✅ **SQLite数据库**：无需额外安装数据库软件，数据安全可靠
- ✅ **智能日期选择器**：美观的日历控件，支持快速选择
- ✅ **数据备份恢复**：完善的备份机制，防止数据丢失

## 📦 文件说明

- `InventoryPro.exe` - 主程序（约23MB，单文件可执行程序）
- `data/inventory.db` - 数据库文件（首次运行自动创建）
- `data/bak/` - 数据备份目录（自动创建）
- `settings.json` - 系统配置文件（自动创建）

## 🛠️ 安装与运行

### 1. 系统要求
- Windows 10/11 (64位)
- 内存：至少4GB
- 硬盘空间：100MB

### 2. 运行步骤
1. 将 `InventoryPro.exe` 复制到任意文件夹
2. 双击运行程序
3. 使用默认账号登录：
   - 用户名：**admin**
   - 密码：**123456**

### 3. 首次运行
- 程序会自动创建 `data` 目录和数据库文件
- 自动创建默认管理员账号（admin/123456）
- 可在"系统设置"中修改系统名称、公司信息等

## 📚 功能模块详细说明

### 🔐 登录系统

**功能特点**：
- 用户名密码登录
- 密码MD5加密存储，安全可靠
- 可选择"记住密码"，下次自动填充用户名
- 登录窗口居中显示，界面美观

**默认账号**：
- 用户名：`admin`
- 密码：`123456`

### 📊 仪表盘

**统计卡片**：
- **商品总数**：系统中所有有效商品的数量
- **今日入库**：当天入库的商品总数量
- **今日出库**：当天出库的商品总数量
- **库存预警**：低于最小库存的商品数量

**最近交易记录**：
- 实时展示最新的出入库明细
- 显示商品名称、规格、数量、单价、金额等信息
- 自动刷新，数据实时更新

### 📥 入库管理

**新增入库单**：
- 填写入库单基本信息：
  - 供应商（必填）
  - 仓库（必填，下拉选择）
  - 入库日期（支持日期选择器）
  - 备注（可选）
- 添加商品明细：
  - 点击"添加商品"按钮选择商品
  - 输入数量、单价（必填）
  - 输入批次号（可选）
  - 自动计算金额和总金额
  - 可添加多个商品
  - 可删除已添加的商品
- 保存后自动更新库存

**查询筛选**：
- 按日期范围查询（支持日期选择器）
- 按入库单号模糊搜索
- 按供应商筛选
- 显示商品编码、商品名称、规格型号、数量等详细信息

**删除功能**：
- 可删除选中的入库单
- 删除时会自动调整库存
- 删除前会提示确认

### 📤 出库管理

**新增出库单**：
- 填写出库单基本信息：
  - 客户（必填）
  - 仓库（必填，下拉选择）
  - 出库日期（支持日期选择器）
  - 备注（可选）
- 添加商品明细：
  - 点击"添加商品"按钮选择商品
  - **只显示当前仓库有库存的商品**
  - 输入出库数量（不能超过当前库存）
  - 输入单价（必填）
  - 自动计算金额和总金额
  - 可添加多个商品
  - 可删除已添加的商品
- 保存时自动检查库存充足性，防止负库存
- 保存后自动扣减库存

**查询筛选**：
- 按日期范围查询（支持日期选择器）
- 按出库单号模糊搜索
- 按客户筛选
- 显示商品编码、商品名称、规格型号、数量等详细信息

**删除功能**：
- 可删除选中的出库单
- 删除时会自动恢复库存
- 删除前会提示确认

### 📦 库存查询

**多条件查询**：
- 商品名称/编码：模糊搜索
- 规格型号：精确匹配
- 仓库：下拉选择筛选
- 库存状态：全部/正常/库存不足/积压

**库存显示**：
- 实时库存数量
- 库存状态颜色标识：
  - 🔴 **红色背景**：库存不足（低于最小库存）
  - 🟡 **黄色背景**：库存积压（超过最大库存）
  - 🟢 **绿色背景**：库存正常
- 显示商品编码、名称、规格、仓库、当前库存、最小/最大库存等信息

**导出功能**：
- 支持导出当前查询结果的库存数据
- 导出为CSV格式，可用Excel打开

**删除功能**：
- 可删除选中的库存记录（谨慎操作）

**初始化数据**（⚠️ 危险操作）：
- 清空所有数据（商品、入库单、出库单、库存等）
- 需要输入初始化密码（默认：123）
- 初始化密码可在"系统设置"→"基本设置"中修改
- 初始化前建议先备份数据

### 📝 商品管理

**新增商品**：
- 商品编码（必填、唯一）
- 商品名称（必填）
- 规格型号（可选）
- 单位（下拉选择：个/箱/件/公斤/吨等）
- 采购价（可选）
- 销售价（可选）
- 最小库存（可选，用于预警）
- 最大库存（可选，用于预警）
- 分类（可选）

**商品列表**：
- 显示所有有效商品及其库存信息
- 支持多条件查询筛选
- 显示商品编码、名称、规格、单位、采购价、销售价、库存等信息

**导入商品**：
- 支持从CSV文件批量导入商品
- CSV格式：商品编码,商品名称,规格型号,单位,采购价,销售价,最小库存,最大库存
- 自动处理重复商品（根据商品编码判断）
- 导入时显示成功和失败的数量

**导出商品**：
- 导出所有有效商品到CSV文件
- 包含商品编码、名称、规格、单位、价格、库存预警等信息
- 可用Excel打开和编辑

**删除商品**：
- 软删除商品（设置状态为无效）
- 删除前会检查是否有库存，有库存的商品不允许删除
- 已删除的商品不会在商品列表中显示，但历史记录仍保留

### 📈 统计报表

**报表类型**：
- **销售报表**：统计出库数据，按时间范围汇总
- **采购报表**：统计入库数据，按时间范围汇总
- **库存报表**：统计当前库存情况
- **利润报表**：统计销售利润（销售金额-成本）

**时间范围**：
- 支持自定义起始日期和结束日期
- 点击日期输入框弹出美观的日期选择器
- 也可以手动输入日期（格式：YYYY-MM-DD）
- 日期选择器支持快速选择：今天、昨天、一周前、一月前等

**生成报表**：
- 点击"生成报表"按钮
- 根据选择的报表类型和时间范围自动生成数据
- 显示详细的报表内容（日期、单号、客户/供应商、商品信息、数量、单价、金额等）

**导出Excel**：
- 支持将当前生成的报表导出为Excel文件
- Excel文件包含格式化的表头（蓝色背景、白色字体）
- 自动调整列宽，便于阅读
- 需要安装openpyxl库（打包时已包含）

### ⚙️ 系统设置

#### 基本设置
- **系统名称**：自定义系统名称
- **公司名称**：设置公司信息
- **初始化密码**：设置初始化数据功能的密码（默认：123）
- 点击"保存设置"保存配置

#### 用户管理
- **添加用户**：
  - 输入用户名（必填、唯一）
  - 输入密码（必填）
  - 确认密码（必填）
  - 新建用户默认为"普通用户"角色
  - 只有管理员可以添加用户
  
- **删除用户**：
  - 选中用户后点击"删除用户"
  - 不能删除管理员账号（admin）
  - 不能删除当前登录的用户
  - 删除前会提示确认
  
- **编辑用户**：
  - 双击用户列表中的用户
  - 可以修改用户密码
  - 只有管理员可以编辑用户

- **用户列表**：
  - 显示所有用户信息（用户名、角色、创建时间）

#### 权限设置
- **选择用户**：从下拉列表选择要设置权限的用户（只显示普通用户）
- **权限项**：
  - ✅ 仪表盘：是否可查看仪表盘
  - ✅ 入库管理：是否可使用入库功能
  - ✅ 出库管理：是否可使用出库功能
  - ✅ 库存查询：是否可查询库存
  - ✅ 商品管理：是否可管理商品
  - ✅ 统计报表：是否可查看报表
  - ✅ 系统设置：是否可进入系统设置（管理员默认拥有所有权限）
- 勾选/取消勾选权限项后自动保存
- 权限设置立即生效，重新登录菜单会相应显示/隐藏

#### 数据备份
- **立即备份数据**：
  - 一键备份当前数据库
  - 备份文件保存在 `data/bak` 目录
  - 备份文件名包含时间戳：`inventory.db_YYYY-MM-DD_HH-MM-SS.bak`
  - 备份成功后会提示备份文件路径

- **恢复数据**：
  - 选择之前备份的数据库文件
  - 恢复前会自动备份当前数据库（防止误操作）
  - 恢复后需要重启程序才能生效
  - ⚠️ 恢复数据会覆盖当前所有数据，请谨慎操作

- **退出备份提示**：
  - 退出程序时会自动提示是否备份数据
  - 选择"是"：先备份再退出
  - 选择"否"：直接退出
  - 选择"取消"：取消退出，继续使用

## 💡 使用技巧

### 1. 快速操作
- **Tab页管理**：
  - 可以同时打开多个功能页面（仪表盘、入库、出库等）
  - 每个Tab页可以独立操作
  - 关闭Tab页：
    - 右键点击Tab标签，选择"关闭"、"关闭其他"、"关闭全部"
    - 双击Tab标签关闭
    - 中键点击Tab标签关闭
    - 按 `Ctrl+W` 关闭当前Tab
  - Dashboard（仪表盘）Tab不能关闭，如果关闭了会自动重新打开

- **日期快速选择**：
  - 点击日期输入框或日期按钮弹出日期选择器
  - 日期选择器支持快速选择：今天、昨天、一周前、一月前
  - 也可以点击日历中的日期直接选择

- **菜单选中状态**：
  - 左侧菜单会根据当前打开的Tab页自动高亮
  - 高亮颜色为蓝色（#2980B9），便于识别当前位置

### 2. 数据安全
- **定期备份**：
  - 建议每天备份一次数据
  - 可以在"系统设置"→"数据备份"中手动备份
  - 退出程序时也会提示备份
  
- **数据存储位置**：
  - 数据库文件：`data/inventory.db`
  - 备份文件：`data/bak/inventory.db_*.bak`
  - 配置文件：`settings.json`
  - 登录信息：`login.json`（如果选择了记住密码）

- **迁移数据**：
  - 复制整个程序目录（包含 `data` 目录）到新位置即可
  - 或只复制 `data` 目录到新程序的 `data` 目录

### 3. 性能优化
- **大量数据操作**：
  - 大量商品导入建议分批进行
  - 查询时缩小时间范围可以加快速度
  - 定期清理过期单据可以提高查询速度

- **库存初始化**：
  - 如果数据出现混乱，可以使用"库存查询"页面的"初始化数据"功能
  - 初始化前务必先备份数据
  - 初始化会清空所有数据，包括商品、单据、库存等

### 4. 权限管理
- **管理员权限**：
  - 默认admin账号拥有所有权限
  - 管理员可以看到所有菜单
  - 管理员可以管理用户和权限

- **普通用户权限**：
  - 普通用户默认没有权限，需要在"权限设置"中分配
  - 可以根据实际工作需要分配相应权限
  - 权限设置后需要重新登录才能生效

## 🔧 常见问题

### Q1：忘记密码怎么办？
**A**：请联系系统管理员重置密码，或使用默认账号admin登录（密码：123456）。如果是管理员忘记密码，可以直接修改数据库中的密码字段（MD5加密后的值）。

### Q2：数据丢失怎么办？
**A**：
1. 检查 `data/bak` 目录是否有备份文件
2. 在"系统设置"→"数据备份"→"恢复数据"中选择备份文件恢复
3. 如果备份文件损坏，可以使用SQLite工具尝试修复数据库
4. **建议**：每天备份一次，重要操作前先备份

### Q3：程序无法启动？
**A**：
1. 确保是Windows 10/11系统
2. 右键点击程序，选择"以管理员身份运行"
3. 检查是否有杀毒软件拦截
4. 检查程序所在目录是否有写入权限

### Q4：数据库损坏？
**A**：
1. 先尝试从备份恢复（推荐）
2. 使用SQLite工具（如DB Browser for SQLite）尝试修复
3. 如果无法修复，只能从最近的备份恢复

### Q5：初始化数据功能如何使用？
**A**：
1. 进入"库存查询"页面
2. 点击"初始化数据"按钮
3. 输入初始化密码（默认：123，可在系统设置中修改）
4. 确认操作，系统会清空所有数据
5. ⚠️ **注意**：此操作不可逆，请先备份数据

### Q6：如何批量导入商品？
**A**：
1. 准备CSV文件，格式为：商品编码,商品名称,规格型号,单位,采购价,销售价,最小库存,最大库存
2. 进入"商品管理"页面
3. 点击"导入商品"按钮
4. 选择CSV文件
5. 系统会自动导入，显示成功和失败的数量

### Q7：Tab页太多如何关闭？
**A**：
- 右键点击Tab标签，选择"关闭全部"可以关闭所有Tab（Dashboard除外）
- 或选择"关闭其他"只保留当前Tab

### Q8：权限设置不生效？
**A**：
- 权限设置后需要重新登录才能生效
- 管理员账号不受权限限制，始终可以看到所有菜单
- 检查是否正确选择了用户并勾选了权限项

## 📞 技术支持

如有问题，请提供：
1. 错误截图
2. 详细的操作步骤
3. 系统版本信息（Windows版本）
4. 数据库文件（如可能）

## 📄 技术说明

- **开发语言**：Python 3.x
- **GUI框架**：Tkinter
- **数据库**：SQLite3
- **图像处理**：PIL/Pillow
- **报表导出**：openpyxl（Excel导出）
- **打包工具**：PyInstaller

## 🔒 安全说明

- 用户密码使用MD5加密存储
- 数据库文件建议定期备份
- 不要随意修改数据库文件
- 建议为管理员账号设置强密码
- 初始化数据功能需要密码保护，建议修改默认密码

---

**进销存管理系统专业版 - 让库存管理更简单、更专业！** 🎉
